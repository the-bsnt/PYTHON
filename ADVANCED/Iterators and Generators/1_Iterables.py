#
# * Iterables:

# ^--> Iterables are objects capable of returning their members one at a time – they can be iterated over.

# ^--> Lists, tuples, sets, strings, dictionaries

# ^--->An iterable is any object that can be looped over or iterated over in a loop, meaning you can go through its elements one by one.

# ^-->Implements the __iter__() method, which returns an iterator.
